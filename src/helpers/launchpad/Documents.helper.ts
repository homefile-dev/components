import { SelectItemI } from '../../interfaces/inputs/Select.interface'

export const CreateDocument = {
  handleDateRange: (date: string) => null,
  handleSelectHome: (form: SelectItemI | string) => null,
  homes: null,
  initialHome: null,
}
